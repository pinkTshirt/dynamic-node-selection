# Dynamic Node Selection (Hybrid Polynomial Root-Finder)

- 'local_code.py' runs an interactive management terminal locally
- github actions saves an artifact giving the end results of each degree polynomial
